import { EmptyProps } from '../types';

const Main: React.FC<EmptyProps> = (): JSX.Element => {
  return (
    <main className="main">
      <p>Main Section</p>
    </main>
  );
};

export default Main;
